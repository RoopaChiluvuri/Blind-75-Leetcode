def countDistinctTriangles(arr):
  distinct_triangles = set()

  for triangle in arr:
    canonical_form  = tuple(sorted(triangle))
    distinct_triangles.add(canonical_form)
    
  return len(distinct_triangles)


# Time Complexity is O(n)
# Space Complexity is O(n)
